import { ReturnvaluePipe } from './returnvalue.pipe';

describe('ReturnvaluePipe', () => {
  it('create an instance', () => {
    const pipe = new ReturnvaluePipe();
    expect(pipe).toBeTruthy();
  });
});
