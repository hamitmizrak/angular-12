import { LowerTextPipe } from './lower-text.pipe';

describe('LowerTextPipe', () => {
  it('create an instance', () => {
    const pipe = new LowerTextPipe();
    expect(pipe).toBeTruthy();
  });
});
